# template for babel-plugin

This is a simple template to create babel plugin.

### dependencies
- build with `unbuild`
- write plugin in `typescript`
- types and utils with `@babel/types`、`@babel/core`
